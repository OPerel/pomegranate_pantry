export enum ROUTES {
  ROOT = '/',
  ADMIN = '/admin',
  LOGIN = '/login',
  USER = '/user',
  ORDER = '/admin/order'
}

export enum ROLES {
  USER = 'user',
  ADMIN = 'rimon'
}

export enum LOCATIONS {
  TA = 'TA',
  PH = 'PH'
}

export enum UNIT_TYPE {
  UNIT = 'unit',
  KG = 'Kg'
}